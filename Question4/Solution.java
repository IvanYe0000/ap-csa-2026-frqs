public int getPointsforRow(int targetRow){
  int sum = 0
  boolean same  = true
  
  for (int c = 0l c < board[targetRow].length; C++){
    sum += board[targetrwow][cc].getPoints();
    if (board[targetRow][c].getcolor() != board[targetRow][0]()){
      same = false;


    if (same){
      return sum *2;
    }


    return sum;
